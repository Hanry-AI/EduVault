<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/bg_gradient_endless_river"
    android:paddingBottom="56dp">

    <!-- ================= TOP NAVIGATION BAR ================= -->
    <!-- Đã thêm layout_marginTop="36dp" để né thanh trạng thái (Status Bar) -->
    <LinearLayout
        android:id="@+id/topNavBar"
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:layout_marginTop="15dp"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingHorizontal="16dp"
        android:background="@android:color/transparent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <!-- 1. Icon Hamburger Menu -->
        <ImageView
            android:id="@+id/imgMenuBurger"
            android:layout_width="28dp"
            android:layout_height="28dp"
            android:src="@android:drawable/ic_menu_sort_by_size"
            android:rotation="-180"
            app:tint="#FFFFFF" />

        <!-- 2. Logo: Đổi tên thành thương hiệu chính chủ EduVault -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="12dp"
            android:text="EduVault"
            android:textColor="#FFFFFF"
            android:textSize="24sp"
            android:textStyle="bold" />

        <!-- Khoảng trống đẩy các icon bên phải dạt sang lề -->
        <View
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1" />

        <!-- 3. Icon User Profile -->
        <ImageView
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:layout_marginEnd="16dp"
            android:src="@drawable/ic_menu_person"
            app:tint="#FFFFFF" />

        <!-- 4. Cờ Việt Nam -->
        <LinearLayout
            android:id="@+id/layoutLanguage"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:padding="8dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:clickable="true"
            android:focusable="true"
            android:layout_marginEnd="4dp">

            <!-- Lá cờ hiện tại -->
            <TextView
                android:id="@+id/tvCurrentFlag"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="🇻🇳"
                android:textSize="20sp"
                android:layout_marginEnd="4dp"/>

            <!-- Mũi tên -->
            <ImageView
                android:layout_width="16dp"
                android:layout_height="16dp"
                android:src="@android:drawable/arrow_down_float"
                app:tint="#FFFFFF" />
        </LinearLayout>
    </LinearLayout>

    <!-- ================= TITLE ================= -->
    <TextView
        android:id="@+id/tvHeaderTitle"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="24dp"
        android:layout_marginHorizontal="24dp"
        android:text="Biến Ghi Chú\nThành Giá Trị"
        android:textColor="#FFFFFF"
        android:textSize="36sp"
        android:textStyle="bold"
        android:gravity="center"
        android:letterSpacing="0.02"
        android:lineSpacingExtra="6dp"
        app:layout_constraintTop_toBottomOf="@id/topNavBar"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- ================= SUBTITLE ================= -->
    <TextView
        android:id="@+id/tvHeaderSubtitle"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="12dp"
        android:layout_marginHorizontal="32dp"
        android:text="Tìm tài liệu chất lượng cao từ sinh viên cùng trường."
        android:textColor="#FFFFFF"
        android:alpha="0.9"
        android:textSize="14sp"
        android:gravity="center"
        android:lineSpacingExtra="4dp"
        app:layout_constraintTop_toBottomOf="@id/tvHeaderTitle"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- ================= SEARCH ================= -->
    <androidx.cardview.widget.CardView
        android:id="@+id/cvSearchContainer"
        android:layout_width="0dp"
        android:layout_height="58dp"
        android:layout_marginTop="28dp"
        android:layout_marginHorizontal="24dp"
        app:cardCornerRadius="30dp"
        app:cardElevation="10dp"
        app:layout_constraintTop_toBottomOf="@id/tvHeaderSubtitle"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:paddingHorizontal="20dp">

            <EditText
                android:id="@+id/edtSearchInput"
                android:layout_width="0dp"
                android:layout_height="match_parent"
                android:layout_weight="1"
                android:background="@android:color/transparent"
                android:hint="Tìm môn học, tài liệu..."
                android:textColor="#333333"
                android:textColorHint="#9E9E9E"
                android:textSize="15sp"
                android:maxLines="1"
                android:inputType="text" />

            <ImageView
                android:id="@+id/imgSearchIcon"
                android:layout_width="22dp"
                android:layout_height="22dp"
                android:layout_marginStart="8dp"
                android:src="@android:drawable/ic_menu_search"
                app:tint="#666666" />
        </LinearLayout>
    </androidx.cardview.widget.CardView>

    <!-- ================= BUTTON ================= -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnExploreNotes"
        android:layout_width="wrap_content"
        android:layout_height="48dp"
        android:layout_marginTop="20dp"
        android:text="Khám phá ngay"
        android:textAllCaps="false"
        android:textColor="#FFFFFF"
        android:textSize="15sp"
        android:textStyle="bold"
        android:gravity="center"
        android:includeFontPadding="false"
        android:paddingHorizontal="36dp"
        app:backgroundTint="#6200EA"
        app:cornerRadius="24dp"
        app:layout_constraintTop_toBottomOf="@id/cvSearchContainer"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>